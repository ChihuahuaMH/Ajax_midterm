<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>EJS example</title>
	</head>
	<body>
		<h1><%=username%></h1>
		<%- interest %>

		<ul>
			<% for (var i=0;i<item.length;i++) { %>
			<li><%=item[i] %></li>
			<% } %>
		</ul>
	</body>
</html>
